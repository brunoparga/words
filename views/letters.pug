extends layouts/main-layout

block content
  h1 Welcome to the Words Game!
  p In this game, you have to find the longest word you can, using only the letters given below.
  p Using rarer letters (like 'Q') will give you more points!
  .letters-container
    each letter in letters
      .letter #{letter}
  form.word(action="/word", method="post")
    label(for="word") Your word:
    input(type="text", name="word", id="word" placeholder="just start typing...")
    input(type="hidden", name="letters", value=letters)
block script
  script(src="js/typing.js")
